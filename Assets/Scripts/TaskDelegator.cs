using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TaskDelegator : MonoBehaviour
{
   /*
    //Managers in Application
    [Header("Managers")]
    [SerializeField] StateManager _stateManager;
    [SerializeField] WindManager _windManager;
    [SerializeField] UIManager _uiManager;
    [SerializeField] AudioManager _audioManager;
    [SerializeField] DebrisManager _debrisManager;
    [SerializeField] ParticleManager _particleManager;
    [SerializeField] SkyManager _skyManager;
    [SerializeField] TreeManager _treeManager;
    [SerializeField] VideoManager _videoManager;
    [SerializeField] WaterManager _waterManager;

    private void OnEnable()
    {
        StateManager.OnStateChange.AddListener(ProcessEvent);
    }
    private void OnDisable()
    {
        StateManager.OnStateChange.RemoveListener(ProcessEvent);
    }
    private void ProcessEvent()
    {
        switch (_stateManager._currentState)
        {
            case States.WelcomeState:
                Enter_WelcomeState();
                break;
            case States.PreHurricaneState:
                Enter_PreHurricaneState();
                break;
            case States.OuterBandsState:
                Enter_OuterBandsState();
                break;
            case States.Category1State:
                Enter_Category1State();
                break;
            case States.Category2State:
                Enter_Category2State();
                break;
            case States.Category3State:
                Enter_Category3State();
                break;
            case States.Category4State:
                Enter_Category4State();
                break;
            case States.EyewallState:
                Enter_EyewallState();
                break;
            case States.StormSurgeStartState:
                Enter_StormSurgeStartState();
                break;
            case States.StormSurgeMaxState:
                Enter_StormSurgeMaxState();
                break;
            case States.StormSurgeRecedingState:
                Enter_StormSurgeRecedingState();
                break;
            case States.AftermathState:
                Enter_AftermathState();
                break;
            case States.CompletionState:
                Enter_CompletionState();
                break;
        }
    }
    private void DelegateWindManager()
    {
        _windManager.
    }
   */
}
